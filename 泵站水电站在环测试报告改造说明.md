# 泵站水电站在环测试报告改造说明

## 给接手 agent 的一句话任务

本项目当前已经能围绕 Hydros Engine 仿真结果生成水位、流量、闸门开度、分水口流量、渠道纵剖面等 HTML/Markdown 报告。接下来需要在现有报告体系上扩展“泵站、水电站对象的调度控制在环测试报告”，用于展示调度控制动作、仿真响应、目标跟踪效果、约束违规、控制性能和在环测试结论。

## 项目定位

`hydros-engine-skill` 是 Skill 层项目，不是 MCP 服务本身。

- 线上 MCP 入口：`https://mcp.hydroos.pub/`
- 通用主线 Skill（仅作参考，不在本次改造范围）：`skills/hydros-engine-skill-executor/`
- 本次改造限定目录：
  - `skills/hydros-engine-skill-report-xuhonghe/`
  - `skills/hydros-engine-skill-report-daduhe/`
- 可参考的主线报告脚本：`skills/hydros-engine-skill-executor/scripts/build_timeseries_report.py`
- 可参考的主线 HTML 模板：`skills/hydros-engine-skill-executor/assets/hydros-report-template/index.html`
- 可参考的主线报告生成指南：`skills/hydros-engine-skill-executor/references/report-generation-guide.md`
- 可参考的主线数据契约说明：`skills/hydros-engine-skill-executor/references/hydros-data-contract.md`

注意：`skills/hydros-engine-skill-executor/` 是通用主线能力，本次不要直接改它。对方 agent 应分别在 `hydros-engine-skill-report-xuhonghe`、`hydros-engine-skill-report-daduhe` 两个目录内落地泵站/水电站调度控制在环测试报告能力；如确实发现需要抽象到主线，应先单独提出方案，不要直接修改主线。

现有报告的默认对象和指标主要是：

| 对象类型 | 当前含义 | 常见指标 |
| --- | --- | --- |
| `CrossSection` | 断面 | `water_level`、`water_flow` |
| `Gate` | 闸门设备 | `gate_opening` |
| `GateStation` | 闸站业务对象 | 聚合其下断面和闸门 |
| `DisturbanceNode` | 分水口/退水闸 | `water_flow` |
| `UnifiedCanal` / `Pipe` | 渠段/管段 | 用于纵剖面和拓扑标注 |

## 目标报告形态

新增的报告不是只看“仿真是否完成”，而是面向“调度控制在环测试”：

1. 控制器或调度策略在仿真过程中发出控制动作。
2. 泵站、水电站对象状态随控制动作变化。
3. 报告评估控制目标是否达成、约束是否满足、响应是否稳定。
4. 报告能回答“这套控制策略能不能用、哪里不稳、下一步应该调什么”。

建议报告标题使用：

```text
泵站/水电站调度控制在环测试报告
```

## 必须覆盖的业务对象

### 泵站对象

建议兼容以下对象类型命名，具体以结果文件和 `objects.yaml` 为准：

| 建议对象类型 | 中文名 | 说明 |
| --- | --- | --- |
| `PumpStation` | 泵站 | 泵组聚合业务对象 |
| `Pump` | 水泵/泵机组 | 单台泵或单个泵组控制单元 |
| `PumpUnit` | 泵机组 | 如果模型用机组粒度表达，按机组处理 |

建议识别的指标：

| 指标 code | 中文名 | 单位建议 | 报告用途 |
| --- | --- | --- | --- |
| `pump_flow` / `water_flow` | 抽排/输水流量 | m³/s | 输出能力、目标跟踪 |
| `pump_head` / `head` | 扬程 | m | 工况判断 |
| `pump_power` / `power` | 功率 | kW / MW | 能耗与负荷 |
| `pump_speed` / `speed` | 转速 | rpm / % | 控制动作响应 |
| `pump_status` / `unit_status` | 启停状态 | 0/1 | 启停时序 |
| `pump_opening` / `valve_opening` | 阀门/导叶开度 | % / m | 控制输出 |
| `energy_consumption` | 能耗 | kWh / MWh | 调度经济性 |

### 水电站对象

建议兼容以下对象类型命名：

| 建议对象类型 | 中文名 | 说明 |
| --- | --- | --- |
| `HydropowerStation` | 水电站 | 电站聚合业务对象 |
| `HydroUnit` | 水轮发电机组 | 单台机组 |
| `Turbine` | 水轮机 | 如果模型按水轮机表达 |
| `Generator` | 发电机 | 如果模型区分水轮机和发电机 |

建议识别的指标：

| 指标 code | 中文名 | 单位建议 | 报告用途 |
| --- | --- | --- | --- |
| `generation_flow` / `turbine_flow` / `water_flow` | 发电流量 | m³/s | 出力来源 |
| `power` / `active_power` | 有功功率 | MW | 负荷跟踪 |
| `target_power` | 目标功率 | MW | 跟踪误差 |
| `head` / `net_head` | 水头/净水头 | m | 工况判断 |
| `unit_status` | 机组启停状态 | 0/1 | 开停机过程 |
| `guide_vane_opening` | 导叶开度 | % | 控制动作 |
| `reservoir_level` / `forebay_level` | 上游/前池水位 | m | 水位约束 |
| `tailwater_level` | 尾水位 | m | 水头计算 |
| `spillage_flow` | 弃水流量 | m³/s | 调度损失 |
| `efficiency` | 效率 | % | 运行质量 |

## 在环测试报告需要新增的核心概念

### 控制事件

报告必须显式展示控制事件，而不是只展示结果曲线。

控制事件建议字段：

| 字段 | 说明 |
| --- | --- |
| `event_id` | 事件 ID |
| `event_name` | 事件名称 |
| `event_type` | 事件类型，如启泵、停泵、设定流量、设定功率、限幅、切机 |
| `target_object_id` | 作用对象 ID |
| `target_object_name` | 作用对象名称 |
| `target_object_type` | 作用对象类型 |
| `trigger_step` | 触发步 |
| `trigger_time` | 触发时间 |
| `command_metric` | 控制量，如目标流量、目标功率、启停状态 |
| `command_value` | 控制指令值 |
| `source` | 指令来源，如默认事件、调度策略、人工注入、MPC |

报告中至少要有：

- 控制事件时间线
- 控制事件与结果曲线联动标记
- 每个关键事件后的响应摘要

### 控制目标

报告需要支持目标线或目标区间。典型目标：

| 目标类型 | 示例 |
| --- | --- |
| 流量目标 | 泵站出口流量跟踪 30 m³/s |
| 功率目标 | 水电站有功出力跟踪 50 MW |
| 水位目标 | 前池水位维持在 62.5 到 63.0 m |
| 约束目标 | 最大扬程、最小生态流量、机组启停次数限制 |

建议在 payload 中新增 `controlTargets`：

```json
{
  "controlTargets": [
    {
      "objectId": 12345,
      "objectName": "示例泵站",
      "metric": "pump_flow",
      "targetType": "setpoint",
      "targetValue": 30,
      "unit": "m³/s",
      "startStep": 10,
      "endStep": 120
    }
  ]
}
```

### 控制评价指标

泵站/水电站在环测试报告至少要输出以下评价指标：

| 指标 | 含义 |
| --- | --- |
| 跟踪 RMSE | 实际值相对目标值的均方根误差 |
| MAE | 平均绝对误差 |
| 最大偏差 | 最大目标偏离 |
| 稳态偏差 | 响应稳定后与目标的平均偏差 |
| 超调量 | 超过目标或约束上限的幅度 |
| 调节时间 | 控制动作后进入允许误差带所需时间 |
| 约束违规次数 | 水位、流量、功率、启停次数等约束违规 |
| 启停次数 | 泵/机组开停机频次 |
| 能耗或发电量 | 泵站看能耗，水电站看发电量 |
| 单位水量能耗 / 单位水量发电 | 经济性或效率指标 |

如果结果文件里没有目标值，则报告必须说明“缺少目标轨迹，只能做状态响应分析，不能计算目标跟踪误差”。

## 建议改造文件

以下路径以两个限定目录为准，分别改：

```text
skills/hydros-engine-skill-report-xuhonghe/
skills/hydros-engine-skill-report-daduhe/
```

不要修改：

```text
skills/hydros-engine-skill-executor/
```

### 1. 数据契约文档

分别修改：

```text
skills/hydros-engine-skill-report-xuhonghe/references/hydros-data-contract.md
skills/hydros-engine-skill-report-daduhe/references/hydros-data-contract.md
```

新增泵站、水电站对象类型和指标说明。重点不是枚举所有可能 code，而是给出兼容策略：

- 对 `object_type` 做同义归类：泵站类、水电站类、机组类。
- 对 `metrics_code` 做语义归类：状态、控制指令、目标、流量、功率、水头、水位、能耗。
- 未识别指标保留原始 code，在 tooltip 和详情表里展示，不要丢弃。

### 2. 报告生成脚本

分别修改：

```text
skills/hydros-engine-skill-report-xuhonghe/scripts/build_timeseries_report.py
skills/hydros-engine-skill-report-daduhe/scripts/build_timeseries_report.py
```

建议新增或拆分函数：

| 函数建议 | 职责 |
| --- | --- |
| `classify_control_object_type()` | 将 PumpStation、Pump、HydropowerStation、HydroUnit 等归类 |
| `classify_control_metric()` | 将指标归为流量、功率、状态、目标、能耗等 |
| `build_control_series()` | 生成泵站/水电站曲线 payload |
| `build_control_events()` | 从场景事件、结果事件或外部事件文件构造事件时间线 |
| `compute_control_kpis()` | 计算 RMSE、MAE、调节时间、超调、违规等 |
| `build_control_report_payload()` | 将控制相关 payload 合并进现有 `reportData` |

不要把泵站/水电站逻辑硬塞进闸门图表逻辑里。现在模板里 gate chart 和业务筛选已经较重，建议新增控制对象独立区块。

### 3. HTML 模板

分别修改：

```text
skills/hydros-engine-skill-report-xuhonghe/assets/hydros-report-template/index.html
skills/hydros-engine-skill-report-daduhe/assets/hydros-report-template/index.html
```

新增一个“调度控制在环测试”章节，建议包含：

1. 控制测试总览卡片
   - 控制对象数
   - 控制事件数
   - 目标跟踪指标数
   - 约束违规数
   - 结论：通过 / 需关注 / 未通过

2. 控制事件时间线
   - 事件名称
   - 触发步/时间
   - 作用对象
   - 指令值

3. 泵站/水电站结果曲线
   - 实际值、目标值、控制指令同轴或分轴展示
   - 鼠标悬停可读 step、时间、实际值、目标值、误差、控制指令
   - 图例可隐藏；隐藏后 y 轴按可见曲线自适应
   - 控制事件在图上用竖线或标记点展示

4. 目标跟踪与约束表
   - 对象
   - 指标
   - RMSE / MAE / 最大偏差
   - 超调
   - 调节时间
   - 违规次数
   - 判定

5. 控制结论与建议
   - 哪些对象控制效果好
   - 哪些对象响应慢、超调大、频繁启停或违反约束
   - 下一步建议调参方向

### 4. 图表生成脚本

如需 PNG 静态图，同时修改：

```text
skills/hydros-engine-skill-report-xuhonghe/scripts/generate_charts.py
skills/hydros-engine-skill-report-daduhe/scripts/generate_charts.py
```

建议新增：

- `chart_control_tracking.png`：目标跟踪曲线
- `chart_control_events.png`：控制事件与响应
- `chart_control_kpis.png`：控制指标对比

如果 HTML 已经提供足够交互能力，PNG 可以作为补充，不要阻塞主报告交付。

### 5. Skill 说明

分别修改：

```text
skills/hydros-engine-skill-report-xuhonghe/SKILL.md
skills/hydros-engine-skill-report-daduhe/SKILL.md
```

新增触发场景：

- 泵站调度
- 水电站调度
- 调度控制
- 在环测试
- 闭环控制验证
- 目标跟踪
- 控制策略评估

并说明在结果报告阶段要优先识别泵站/水电站对象与控制事件。

## 推荐 payload 扩展

在现有 `reportData` 中新增：

```json
{
  "control": {
    "enabled": true,
    "objectGroups": [
      {
        "groupId": "pump_station",
        "label": "泵站",
        "objects": []
      },
      {
        "groupId": "hydropower_station",
        "label": "水电站",
        "objects": []
      }
    ],
    "series": [],
    "events": [],
    "targets": [],
    "kpis": [],
    "constraints": [],
    "summary": {
      "controlObjectCount": 0,
      "eventCount": 0,
      "targetCount": 0,
      "violationCount": 0,
      "verdict": "未评估"
    }
  }
}
```

`series` 建议每条记录包含：

```json
{
  "seriesId": "power|HydroUnit|10001",
  "objectId": 10001,
  "objectName": "1号机组",
  "objectType": "HydroUnit",
  "objectGroup": "hydropower_station",
  "metric": "power",
  "metricLabel": "有功功率",
  "unit": "MW",
  "role": "actual",
  "points": [
    { "step": 0, "time": "2026-01-01 00:00:00", "value": 32.5 }
  ]
}
```

`role` 建议值：

- `actual`：实际响应
- `target`：目标轨迹
- `command`：控制指令
- `constraint_upper`：约束上限
- `constraint_lower`：约束下限

## 控制评价计算建议

### 目标跟踪

同一对象、同一业务指标下，匹配：

- `role=actual`
- `role=target`

按 step 对齐后计算：

```text
error = actual - target
RMSE = sqrt(mean(error^2))
MAE = mean(abs(error))
maxDeviation = max(abs(error))
bias = mean(error)
```

### 调节时间

控制事件发生后，从 `trigger_step` 开始寻找第一次进入误差带并持续若干点的 step。

建议默认：

- 误差带：目标值的 2% 或业务配置阈值
- 持续点数：3 个采样点

如果目标为 0 或缺少目标值，跳过调节时间并写明原因。

### 超调

对有目标值的曲线：

```text
overshoot = max(actual - target)
undershoot = min(actual - target)
```

对有约束的曲线：

```text
upperViolation = max(0, actual - upperLimit)
lowerViolation = max(0, lowerLimit - actual)
```

### 启停次数

对 `unit_status` / `pump_status`：

```text
startCount = count(status 从 0 变 1)
stopCount = count(status 从 1 变 0)
```

如果状态不是 0/1，而是字符串，需要先标准化：

- `on` / `running` / `open` / `启动` / `运行` => 1
- `off` / `stopped` / `close` / `停止` / `停机` => 0

## 报告判定建议

建议输出三个等级：

| 判定 | 条件建议 |
| --- | --- |
| 通过 | 无硬约束违规，主要目标 RMSE 在阈值内，控制动作无异常振荡 |
| 需关注 | 有轻微超调或个别指标偏差大，但无严重约束违规 |
| 未通过 | 存在硬约束违规、频繁启停、目标严重偏离或控制无响应 |

阈值不要写死在模板里。优先使用场景配置、事件配置或用户参数；没有阈值时，报告中写“按默认经验阈值评估”，并把阈值展示出来。

## 实现顺序建议

1. 扩展数据契约文档，明确泵站/水电站对象和指标归类。
2. 在 `build_timeseries_report.py` 中新增对象与指标分类，不影响现有断面、闸门、分水口报告。
3. 先生成 `reportData.control` payload，并把它写入 `data/report.data.js` 和内联 HTML。
4. 在 HTML 模板中新增控制报告章节。
5. 增加控制 KPI 计算和表格。
6. 增加控制事件时间线与曲线标记。
7. 用最小样例 CSV 做回归测试，确保旧水力仿真报告仍可生成。

## 验收标准

### 功能验收

- 给定只包含原有 `CrossSection`、`Gate`、`DisturbanceNode` 的结果文件，旧报告表现不回退。
- 给定包含泵站对象的结果文件，报告出现“泵站调度控制在环测试”内容。
- 给定包含水电站对象的结果文件，报告出现“水电站调度控制在环测试”内容。
- 如果存在目标轨迹，报告计算 RMSE、MAE、最大偏差、调节时间和超调。
- 如果不存在目标轨迹，报告明确说明不能做目标跟踪误差，只做状态响应分析。
- 控制事件能在时间线展示，并能在曲线上标记。
- 图例可隐藏，隐藏后 y 轴按可见曲线自适应。
- 鼠标悬停曲线可读 step、时间、实际值、目标值、误差和控制指令。

### 工程验收

- 不要破坏现有 `simulation_report.html` 生成流程。
- 不要把泵站/水电站逻辑写死到某一个场景 ID。
- 不要用旧缓存或默认样例数据冒充真实结果。
- 生成的 Markdown 文件使用中文名称。
- Python 脚本仍使用 `python3` 执行。
- 无目标、无事件、无约束时，页面应降级展示说明，不应报 JS 错误。

## 最小测试数据建议

可以先构造一个小 CSV 做本地回归，至少包含：

```text
object_id,object_name,object_type,metrics_code,data_index,value
1001,示例泵站,PumpStation,pump_flow,0,20
1001,示例泵站,PumpStation,pump_flow,1,25
1001,示例泵站,PumpStation,target_flow,0,30
1001,示例泵站,PumpStation,target_flow,1,30
1002,1号机组,HydroUnit,power,0,40
1002,1号机组,HydroUnit,target_power,0,45
```

如果真实导出结果没有 `target_*` 指标，则可以先支持外部目标文件参数，例如：

```bash
python3 skills/<hydros-engine-skill-report-xuhonghe 或 hydros-engine-skill-report-daduhe>/scripts/build_timeseries_report.py \
  output/<task>/data/output-series.csv \
  output/<task> \
  --control-targets output/<task>/data/control_targets.json \
  --control-events output/<task>/data/control_events.json
```

这两个参数当前项目未实现，是建议新增能力。

## 不建议做的事

- 不要把泵站、水电站简单归到 `Gate` 或 `DisturbanceNode` 里凑图。
- 不要只画一堆曲线而不解释控制事件和目标跟踪。
- 不要把所有指标塞进同一个坐标轴，功率、流量、水位、状态应分组展示。
- 不要写死 `PumpStation` / `HydropowerStation` 是唯一对象名，真实数据可能有别名。
- 不要把调度控制判定只放在聊天回复里，必须进入 HTML 报告正文。

## 可直接交给 agent 的提示词

```text
你要修改 /Users/gaobingxiang/code/hydros-engine-skill 项目。

目标：在现有 Hydros 仿真报告基础上，新增泵站、水电站对象的调度控制在环测试报告能力。

改动范围硬约束：
- 只在以下两个目录内分别落地：
  1. skills/hydros-engine-skill-report-xuhonghe/
  2. skills/hydros-engine-skill-report-daduhe/
- 不要直接修改 skills/hydros-engine-skill-executor/ 通用主线。
- 如果发现必须抽象到通用主线，先输出方案和影响面，不要自行改主线。

请先阅读：
1. README.md
2. CLAUDE.md
3. skills/hydros-engine-skill-report-xuhonghe/SKILL.md
4. skills/hydros-engine-skill-report-xuhonghe/references/hydros-data-contract.md
5. skills/hydros-engine-skill-report-xuhonghe/references/report-generation-guide.md
6. skills/hydros-engine-skill-report-xuhonghe/scripts/build_timeseries_report.py
7. skills/hydros-engine-skill-report-xuhonghe/assets/hydros-report-template/index.html
8. skills/hydros-engine-skill-report-daduhe/SKILL.md
9. skills/hydros-engine-skill-report-daduhe/references/hydros-data-contract.md
10. skills/hydros-engine-skill-report-daduhe/references/report-generation-guide.md
11. skills/hydros-engine-skill-report-daduhe/scripts/build_timeseries_report.py
12. skills/hydros-engine-skill-report-daduhe/assets/hydros-report-template/index.html
13. 泵站水电站在环测试报告改造说明.md

实现要求：
- 保持现有断面、水位、流量、闸门开度、分水口和纵剖面报告不回退。
- 新增泵站/水电站对象和指标分类。
- 新增 control payload，包含 control objects、series、events、targets、kpis、constraints、summary。
- HTML 报告新增“调度控制在环测试”章节。
- 支持目标跟踪 RMSE、MAE、最大偏差、调节时间、超调、约束违规、启停次数等指标。
- 支持无目标/无事件时的降级说明。
- 图表支持 hover 读数、图例隐藏、隐藏后 y 轴自适应。
- 生成的 Markdown 文件必须使用中文名称。

完成后请给出改动文件、运行命令、验证结果和剩余风险。
```
